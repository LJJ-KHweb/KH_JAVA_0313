Set, Map, 

List<제네릭> 
제네릭 : 타입을 지정해서 컴파일 시점에 체크하는것
1. 다룰 요소의 타입을 제한할수 있음
2. 매번 형 변환하는 절차를 없앨 수 있음 

Hashset
-> Value 값만 저장, index 개념 없음 == 저장순서보장 X , 중복 X

set에 새로운 요소를 추가할 때 마다 equals()와 hashCode()의 반환값을 기준으로 일치하는 요소가 있는지 없는지 판별함
->equals()의 호출결과가 true고 hashCode()의 반환값을 동등 비교 연산자를 통해 비교했을때 값이 일치한다면 --> 동일객체로 판단

VO클래스는 일반적으로 반드시 내부에 equals()와 hashCode()를 오버라이딩 해줘야함

Record 
getter tostring equals hashCode 생성자  다만들어줌

Interator(반복자)
-> 개발자가 구체적인 자료구조를 모르더라도 반복을 시킬수 있는 방법 (List, Set 은 바로 사용가능) (Map계열은 바로 사용 불가능)
hasNext()
->다음 요소에 접근해서 요소 있으면 true 없으면 false를 리턴
 next()
->다음 요소를 가짐

ArrayList -> 배열로
LinkedList -> 노드를 연결
HashSet -> 해시테이블
TreeSet -> 트리구조

Map<Key, Value> 
->순서 보장 X Value값의 경우 중복허용 Key는 중복이 발생하면 덮어씌움(중복 불가)
HashMap


1. 추가 put(K Key, K Value) Map은 항상 key value를 같이 추가해됨
2. get(Object key) : 반환타입은 object 없으면 null을 반환
인자로 전달된 key값에 해당하는 value값을 반환해주는 메소드
key로 value를 찾아옴
3. replace(K key, V value) : key로 요소를 찾아서 value값을 치환해줌
put() replace() 차이점
put은 그냥 바로 추가 replace는 해당 키값이 존재하지 않는다면 추가하지 않음
4. remove(Object key) key값에 해당 요소를 삭제
map함수를 반복을 돌려서 출력할려면 
.forEach((key,value)-> {
			System.out.println(key + " : " + value);
		});
or
map의 key들만 set으로 만들기
Set<String> subSet = subway.keySet();
		Iterator<String> iter = subSet.iterator();
		while(iter.hasNext()) {
			String key = iter.next();
			System.out.println(key + "===" + subway.get(key));
		}

파일은 파일인데 확장자가 .properties 
Map을 상속받음
변경하지 않는 설정정보를 저장할때 씀
상속에 잘못된 예 (면접질문)


Network(네트워크)
-> 통신이 가능한 두 개 이상의 디바이스를 물리적/논리적으로 연결해 놓은 환경

데이터 전송

 Protocol 통신규약

전제 : Internet 환경에서 통신을 하기 위해서 기본적으로 IP Address가 필요함

